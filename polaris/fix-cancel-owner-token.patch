From: polaris-hdr-linux-patches
Subject: fix(nvhttp): session owner cancel ignores stale sessiontoken

--- a/src/nvhttp.cpp	2026-07-16 18:54:06.000000000 +0200
+++ b/src/nvhttp.cpp	2026-07-27 18:46:45.210656117 +0200
@@ -5414,12 +5414,21 @@
     }
 
     const auto session_token = get_arg(args, "sessiontoken", "");
+    // Owner is the paired cert UUID that launched the session. Do not require an
+    // exact sessiontoken for that client — modern clients often send a stale one
+    // and surface 470 "belongs to another client" / "different device" on quit.
+    const bool is_owner = proc::proc.is_session_owner(named_cert_p->uuid);
     const auto shutdown = proc::proc.request_session_shutdown(
       named_cert_p->uuid,
       session_token,
       true,
-      !session_token.empty()
+      !session_token.empty() && !is_owner
     );
+    BOOST_LOG(info) << "cancel: client=["sv << named_cert_p->name
+                    << "] owner="sv << (is_owner ? "yes" : "no")
+                    << " token_sent="sv << !session_token.empty()
+                    << " outcome="sv << static_cast<int>(shutdown.snapshot.outcome)
+                    << " stopped="sv << shutdown.stopped;
     switch (shutdown.snapshot.outcome) {
       case proc::session_stop_outcome_t::allowed:
       case proc::session_stop_outcome_t::no_active_session:
--- a/src/process.cpp	2026-07-16 18:54:06.000000000 +0200
+++ b/src/process.cpp	2026-07-27 18:46:45.211280690 +0200
@@ -6252,7 +6252,9 @@
   bool proc_t::is_session_owner(const std::string &unique_id) {
     auto &sync = session_lifecycle_sync();
     std::lock_guard<std::recursive_mutex> lifecycle_lock(sync.mutex);
-    return !_launch_session || _launch_session->unique_id.empty() || _launch_session->unique_id == unique_id;
+    return !_launch_session ||
+           _launch_session->unique_id.empty() ||
+           boost::iequals(_launch_session->unique_id, unique_id);
   }
 
   bool proc_t::session_uses_virtual_display() {
@@ -6374,10 +6376,11 @@
         }
       );
     snapshot.owned_by_client =
-      _launch_session &&
-      !unique_id.empty() &&
-      !_launch_session->unique_id.empty() &&
-      _launch_session->unique_id == unique_id;
+      // Same rules as is_session_owner (empty owner id → allow; case-insensitive).
+      !_launch_session ||
+      _launch_session->unique_id.empty() ||
+      (!unique_id.empty() &&
+       boost::iequals(_launch_session->unique_id, unique_id));
     snapshot.game = _app_name;
     const bool token_matches = session_stop_token_matches(
       require_exact_token,
