# Archived: pre-topic issue-152 series

**Superseded by** [`polaris/01`–`03`](../../../polaris/README.md) (topic rewrite, 2026-07-13).

This directory is the historical `0001`…`0015` + `combined.patch` stack applied onto
master `2008458`. Kept only for archaeology.

| Series | Note |
|--------|------|
| `0001`…`0006` / `combined.patch` | Upstream-oriented #152 PipeWire portal foundation |
| `0007`…`0008`, `0013`…`0015` | Lea DmaBuf / CUDA / xBGR follow-ups → folded into **01** |
| `0009` | GL DmaBuf import — **never applied** (NVIDIA black) |
| `0010` | Web session persist → **03** |
| `0011`…`0012` | Portal HDR metadata + force gate → **02** |

Do not wire these into `pkgs/polaris-stream`.
